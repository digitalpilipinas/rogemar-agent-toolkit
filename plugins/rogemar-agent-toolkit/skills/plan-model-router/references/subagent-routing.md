# Sub-agent routing

Follow [dispatch-contract.md](dispatch-contract.md). `workflow-orchestrator`
owns delegation and permission contracts; this router supplies qualified model
and effort arguments. Named Forge modes govern workers; the no-mode path retains
parent ceilings. Native role pins and required role contracts must be checked.
Keep requested, expected, tool-accepted, and observed profiles distinct.
