interface:
  display_name: "plan-model-router"
  short_description: "Resolve Forge modes, roles, models, and reasoning."
  default_prompt: "Use $plan-model-router to resolve the requested task profile through the current runtime catalogue."
policy:
  allow_implicit_invocation: true
