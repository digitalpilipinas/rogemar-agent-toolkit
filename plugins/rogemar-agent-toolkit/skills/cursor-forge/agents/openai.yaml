interface:
  display_name: "cursor-forge"
  short_description: "Portable engineering and native capability routing."
  default_prompt: "Use $cursor-forge for this task with the current harness capabilities."
policy:
  allow_implicit_invocation: true
