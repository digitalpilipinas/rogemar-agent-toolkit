interface:
  display_name: "forge-setup"
  short_description: "Configure five Forge modes and exact Default profiles"
  default_prompt: "Use $forge-setup for the requested task."
policy:
  allow_implicit_invocation: true
