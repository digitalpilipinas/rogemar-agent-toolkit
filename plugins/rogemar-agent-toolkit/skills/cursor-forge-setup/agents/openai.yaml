interface:
  display_name: "cursor-forge-setup"
  short_description: "Configure Cursor modes, roles and native model profiles."
  default_prompt: "Use $cursor-forge-setup to inspect or configure Cursor Forge routing."
policy:
  allow_implicit_invocation: true
