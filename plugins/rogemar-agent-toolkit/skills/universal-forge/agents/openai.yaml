interface:
  display_name: "universal-forge"
  short_description: "Portable engineering and native capability routing."
  default_prompt: "Use $universal-forge for this task with the current harness capabilities."
policy:
  allow_implicit_invocation: true
